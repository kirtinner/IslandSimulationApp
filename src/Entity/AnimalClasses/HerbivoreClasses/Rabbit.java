package Entity.AnimalClasses.HerbivoreClasses;

public class Rabbit extends Herbivore {
    public Rabbit(double weight, int maxPerCell, int speed, double foodNeeded) {
        super(weight, maxPerCell, speed, foodNeeded);
    }
}