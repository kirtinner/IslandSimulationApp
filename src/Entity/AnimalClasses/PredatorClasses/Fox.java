package Entity.AnimalClasses.PredatorClasses;

public class Fox extends Predator {
    public Fox(double weight, int maxPerCell, int speed, double foodNeeded) {
        super(weight, maxPerCell, speed, foodNeeded);
    }
}